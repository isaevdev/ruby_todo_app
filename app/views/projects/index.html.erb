<p id="notice"><%= notice %></p>

 <div class="container table-responsive">
    <div class="row">
    <table class="table">
      <thead>
        <tr>
          <th>Name</th>
          <th colspan="3"></th>
        </tr>
      </thead>

        <tbody>

      </tbody>
        <% @projects.each do |project| %>
          <tr>
            <td><%= project.name %></td>
            <td><%= link_to 'Show', project %></td>
            <td><%= link_to 'Edit', edit_project_path(project) %></td>
            <td><%= link_to 'Destroy', project, method: :delete, data: { confirm: 'Are you sure?' } %></td>
            <td><%= link_to 'Add Task', new_task_path(project_id: project.id) %></td>
           </tr>
           <tr>
               <table class="table">
                <tbody>
                <% project.tasks.each do |task| %>
                  <tr>
                    <td><%= task.name %></td>
                  </tr>
                 <% end %>
               </tbody>
               </table>
           </tr>
        <% end %>
      </tbody>
    </table>
  </div>
  <%= link_to 'New Project', new_project_path, :class => "btn btn-info" %></a>
</div>



